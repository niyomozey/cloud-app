apiVersion: apps/v1
kind: Deployment
metadata:
 name: cassandra
spec:
 selector:
  matchLabels:
    app: cassandra
 template:
  metadata:
    labels:
      app: cassandra
  spec:
    containers:
      - name: cassandra
        image: cassandra
        ports:
          - containerPort: 9042
